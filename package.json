{
  "name": "plan-line-to-span-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Plan Line to Span demo. Implementation in progress; see docs/implementation-plan.md.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test \"test/**/*.test.ts\"",
    "test:only": "node --test \"test/**/*.test.ts\"",
    "prototypes": "for f in docs/design/prototypes/dt-*.mjs; do echo \"== $f\"; node \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "ajv": "^8.20.0",
    "typescript": "^7.0.2"
  }
}
